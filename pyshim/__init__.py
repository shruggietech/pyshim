"""
pyshim - A context-aware Python shim for Windows

A deterministic, context-aware Python shim that lets you control which Python 
interpreter is used by apps, projects, and background tools without breaking 
the global environment.
"""

__version__ = "0.1.0"
